package com.hireready.repositories;

import com.hireready.entities.ResponseAudio;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ResponseAudioRepository extends JpaRepository<ResponseAudio, Long> {
}
